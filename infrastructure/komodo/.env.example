# Komodo Environment Variables
# Copy to .env and fill in real values: cp .env.example .env
#
# IMPORTANT: Docker .env files do NOT support inline comments.
# Do NOT put comments on the same line as a value.
# WRONG: MONGO_PASSWORD=secret   # my password
# RIGHT: MONGO_PASSWORD=secret
#
# Generate secrets with:
#   openssl rand -hex 16  (for passwords)
#   openssl rand -hex 32  (for passkeys)

KOMODO_HOST=http://YOUR_SERVER_IP:9120
KOMODO_TITLE=Home Lab
KOMODO_FIRST_SERVER=main
MONGO_USERNAME=admin
MONGO_PASSWORD=
KOMODO_PASSKEY=
KOMODO_LOCAL_AUTH=true
KOMODO_INIT_ADMIN_USERNAME=admin
KOMODO_INIT_ADMIN_PASSWORD=
KOMODO_DATABASE_URI=mongodb://admin:YOUR_MONGO_PASSWORD_HERE@komodo-mongo:27017
# ^ Replace YOUR_MONGO_PASSWORD_HERE with the same value as MONGO_PASSWORD above
